// Legacy contact section - replaced by PremiumContactSection
import PremiumContactSection from "./premium-contact-section";

/**
 * Contact Section Component - Now uses Premium Contact Section
 */
const ContactSection: React.FC = () => {
  return <PremiumContactSection />;
};

export default ContactSection;
